app:
  access-type: jpa
  kafka:
    topic-name: "link_updates"

spring:
  main:
    allow-bean-definition-overriding: true
  jpa:
    hibernate:
      ddl-auto: update
  liquibase:
    enabled: false
